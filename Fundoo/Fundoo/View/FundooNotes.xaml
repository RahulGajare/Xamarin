<?xml version="1.0" encoding="utf-8" ?>
<MasterDetailPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Fundoo.View.FundooNotes"
        NavigationPage.HasBackButton="False"
                 NavigationPage.HasNavigationBar="False">

    <MasterDetailPage.Master >
        <ContentPage  Title="Fundoo Notes">
           

            <ContentPage.Content>
                <StackLayout>
                    <Image Source="FundooNotes.png" WidthRequest="20" HeightRequest="20"/>
                    <!--<Button Text="Notes"/>
                    <Button Text="Reminders"/>-->
                    <ListView x:Name="aboutList" ItemSelected="OnMenuItemSelected">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ImageCell Text="{Binding Text}" Detail="{Binding Detail}"  
                                           ImageSource="{Binding ImagePath}">
                                </ImageCell>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                </StackLayout>
            </ContentPage.Content>
        </ContentPage>
    </MasterDetailPage.Master>
    
    <MasterDetailPage.Detail>
        <ContentPage>
        </ContentPage>
    </MasterDetailPage.Detail>
</MasterDetailPage>